package com.aoopproject.game.samegame.model;

/**
 * Enum representing the possible colors of tiles in SameGame.
 */
public enum TileColor {
    RED, GREEN, BLUE, YELLOW, PURPLE, ORANGE; // Add more colors for higher difficulty
}